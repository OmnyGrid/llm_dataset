# Test fixtures

Shared JSON and phrase-template data for tests. Examples under `example/` remain user-facing demos.

- `curriculum/curriculum.json` — full progressive curriculum manifest (paths point here under `test/fixtures/phrase_templates/`).
- `phrase_templates/` — copy of the example EN template store used by curriculum and phrase-store tests.
