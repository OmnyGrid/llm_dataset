name: llm_dataset
description: Stream-oriented infrastructure for building and querying LLM training datasets.
version: 1.4.0
repository: https://github.com/OmnyGrid/llm_dataset

environment:
  sdk: ^3.13.0

dependencies:
  sqlite3: ^3.5.2

dev_dependencies:
  dependency_validator: ^3.2.3
  lints: ^6.1.0
  test: ^1.31.2
